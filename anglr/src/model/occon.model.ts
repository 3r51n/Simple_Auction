export interface OcconModel
{
    bidders(bidder{id,bid: number,userName: string}[]),
    highestBidder: string, highestBid: number, highestBidder2: string, hightBid2:number, balanceTime:string
    
 
}